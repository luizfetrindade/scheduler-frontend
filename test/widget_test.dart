import 'package:flutter_test/flutter_test.dart';

// Smoke test — kept minimal.
// Feature-level tests live in test/features/ and test/core/.
void main() {
  test('project compiles', () => expect(true, isTrue));
}
